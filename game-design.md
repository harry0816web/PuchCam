# PunchCam 遊戲設計與現況

## 遊戲定位

PunchCam 是一款雙人即時 WebCam 拳擊遊戲。兩位玩家用瀏覽器開啟鏡頭、建立或加入房間，透過上半身姿勢辨識出拳、防禦與閃躲；目標是在回合內造成更多有效傷害。

## 玩家流程

1. 房主建立五碼房間並取得邀請連結；朋友可透過連結自動帶入房間碼，或手動輸入房間碼加入。
2. 兩人允許鏡頭與麥克風權限後，以 PeerJS 建立點對點資料與影音連線。加入方若暫時連不到房主，會自動重試。
3. 兩人各自完成或跳過動作教學；房主在雙方準備完畢後同步倒數三秒。
4. 對戰採三回合制，每回合 60 秒；生命值歸零或時間結束即結束該回合。三回合後，以累計有效命中數決定勝負，或顯示平手。

## 對戰規則

| 動作 | 現行辨識條件 | 效果 |
| --- | --- | --- |
| 直拳 | 手腕速度超過門檻，且手腕到肩膀的距離明顯大於手肘到肩膀的距離 | 造成 10 傷害 |
| 勾拳 | 手腕速度超過門檻，但手臂沒有呈現直拳的明顯伸展 | 造成 14 傷害 |
| 左右閃躲 | 肩膀中心或頭部相對肩膀明顯向左、右偏移 | 可躲避直拳 |
| 下蹲 | 頭部下降至接近肩膀高度 | 可躲避勾拳 |
| 格檔 | 雙手腕皆被可靠追蹤，且舉到畫面上方 32% 內、雙手距離合理 | 傷害降低 80%（直拳 2、勾拳 3） |

只有可靠追蹤到手腕時，玩家才可出拳或格檔；未追蹤到手腕時仍可使用頭部與肩膀進行閃躲。閃躲優先於格檔，因此下蹲時即使雙手抬高也會被判定為下蹲。

## 教學模式

- 每個動作先以循環動畫與文字說明展示。
- 玩家按下「開始練習」後，必須用正式對戰相同的判定規則成功做出兩次，才會自動進到下一個動作。
- 教學順序：直拳、勾拳、左右閃躲、下蹲、格檔。
- 熟悉玩法的玩家可隨時選擇「跳過全部教學」；兩名玩家都完成或跳過後才開始比賽。

## 鏡頭、連線與辨識

- 使用前置鏡頭與麥克風，請求目標畫面為 1280×720。
- 本機與對手鏡頭皆以 16:9 顯示；本機畫面為鏡像，對手畫面維持原始方向。
- 使用 MediaPipe Pose Landmarker，只繪製並使用頭部、肩膀、手肘與手腕的上半身追蹤點，因此可坐著玩。
- 使用 MediaPipe Face Landmarker 追蹤臉部位置、大小、旋轉、眨眼、張嘴與微笑。
- 使用 PeerJS 的 WebRTC 影音通道與資料通道；資料通道同步出拳、閃躲、格檔、教學準備狀態與手腕位置。房主是傷害判定權威端：所有攻擊均由房主套用雙方最新防禦狀態後，再回傳唯一的戰鬥結果。
- 連線前會進行約 45 個影格的姿勢校正，量測肩寬與中立肩膀位置，並以結果調整出拳速度、側閃與下蹲門檻。
- 內建 STUN 直連；若設定 `VITE_TURN_URL`、`VITE_TURN_USERNAME`、`VITE_TURN_CREDENTIAL`，PeerJS 會加用 TURN 中繼以支援嚴格 NAT 網路。
- 本機將 3D 頭套合成到輸出的影像串流，因此對手也能看到頭套效果。

## 視覺與音效

- 預設青蛙 3D 頭套，另可切換小豬與兔子。
- 頭套會依臉部大小套住頭部，並跟隨左右轉頭、上下點頭與側傾；眼睛與嘴巴會對應眨眼、張嘴與笑容。
- 本機畫面顯示上半身追蹤骨架、臉部追蹤狀態與手腕追蹤狀態。
- 對手畫面顯示半透明拳套，位置由對手雙手腕即時同步，協助預判攻擊。
- 出拳時顯示 `KAPOW!`／`WHAM!`、速度線與畫面晃動。
- 直拳會顯示直線拳套軌跡；勾拳會顯示弧形軌跡，並依左右手改變方向。
- 被有效命中時顯示 `HIT!` 的閃光、衝擊線與鏡頭震動；格檔時顯示 `BLOCK!` 的藍黃效果。
- 直拳、勾拳、閃躲、格檔、命中皆有不同的 Web Audio 即時合成音效與震動節奏，並可靜音或調整音量。
- 設定面板可調整音量、靜音、自己的鏡像畫面、追蹤點與對手拳套顯示；頭套可切換為關閉、小青蛙、小豬或兔子。
- 回合中顯示本回合雙方命中數與傷害；開局、回合結束／KO 與最終勝負均會顯示全畫面通知。

## 開發與品質檢查

- Vite + React + TypeScript 前端，可部署為 Vercel 靜態網站。
- 單元測試覆蓋手腕可見度、直拳／勾拳判定、閃躲、格檔、傷害與攻防相剋。
- Playwright E2E 測試覆蓋首頁與邀請連結帶入房間碼。
- GitHub Actions CI 在 PR 與 `main` 推送時執行型別檢查、單元測試、production build 與 Chromium E2E。

## 建議優化項目

### P0：先改善實際可玩性與可信度

1. **房主權威判定已完成。** 房主統一計算傷害並回傳結果；下一步可將含時間戳的動作快照與延遲補償加入判定。
2. **動作校正已完成。** 開始前量測中立姿勢與肩寬以調整閾值；下一步可加入手臂長度與鏡頭距離校正，進一步改善不同鏡頭視角的表現。
3. **TURN 設定介面已完成。** 請在 Vercel 設定三個 `VITE_TURN_*` 環境變數並重新部署，才能真正提供中繼服務；下一步是加入連線診斷與重連按鈕。

### P1：提升對戰回饋

1. **回合回饋已完成。** 已加入開局、回合結束、KO、勝負通知與本回合命中／傷害統計。
2. **攻擊回饋已完成。** 已加入依左右手顯示的直線／弧形拳套軌跡，以及各動作的獨立音效與震動節奏。
3. **設定面板已完成。** 可調整音量、靜音、鏡像、追蹤點與對手拳套顯示。
4. **頭套效能選項已完成一部分。** 已可關閉頭套以降低 WebGL 與串流合成負載。要改用 GLB/VRM 等可載入 3D 資產，仍需要選定模型來源、授權與檔案。

### P2：產品完成度與維運

1. 建立房間過期、重新加入、對手斷線後繼續等待／重新邀請的完整狀態流程。
2. 加入瀏覽器相容性與效能降級策略：無 WebGL 時退回 emoji 頭套，低效能裝置降低模型偵測頻率與串流解析度。
3. 擴充 E2E：模擬兩名玩家的 PeerJS 資料事件、教學進度、回合流程與命中／格檔 UI；並加入動作規則的邊界測試。
4. 針對公開部署新增隱私說明、鏡頭／麥克風用途告知與錯誤回報機制。
